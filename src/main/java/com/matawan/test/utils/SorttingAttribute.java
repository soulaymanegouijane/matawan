package com.matawan.test.utils;

/**
 * This enum represents sorting attributes for specifying the field by which to sort data, such as by name, acronym, or budget.
 */
public enum SorttingAttribute {
    NAME,
    ACRONYM,
    BUDGET,
}
